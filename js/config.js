/* =====================================================================
   Maison Créative - configuration Supabase
   ---------------------------------------------------------------------
   Renseigne les deux valeurs ci-dessous depuis ton tableau de bord :
   Supabase > Project Settings > API
     - SUPABASE_URL      = "Project URL"
     - SUPABASE_ANON_KEY = "anon public" (cette clé est publique, elle est
                            protegee par les regles RLS de la base)

   IMPORTANT : une fois tes vraies cles en place, ne reecris plus ce
   fichier. Tant qu'il contient les valeurs d'exemple, le site fonctionne
   en mode statique (contenu par defaut affiche).
   ===================================================================== */

window.MC_CONFIG = {
  SUPABASE_URL: "https://TON-PROJET.supabase.co",
  SUPABASE_ANON_KEY: "TA_CLE_ANON_PUBLIC"
};
